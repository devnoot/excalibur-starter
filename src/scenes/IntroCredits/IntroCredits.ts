import * as ex from 'excalibur'

export class IntroCredits extends ex.Scene {

    public onInitialize(_engine: ex.Engine) {
        super.onInitialize(_engine)
    }

    public onActivate(_oldScene: ex.Scene, _newScene: ex.Scene) {
        super.onActivate(_oldScene, _newScene)
    }

    public onDeactivate(_oldScene: ex.Scene, _newScene: ex.Scene) {
        super.onDeactivate(_oldScene, _newScene)
    }
}